# Release gate v8
P0 controls now represented in code: Telegram initData verification; Redis server sessions and distributed rate limit; APPROVED server gate; private MinIO/S3 media boundary; staff Telegram allowlist; TOTP second factor; moderation audit; notification outbox; verification retention; upload magic-byte/size validation; CSP/security headers; Alembic initial schema; backup script.
Still required operationally before public launch: HTTPS certificate and hardened reverse proxy; network-isolate Postgres/Redis/MinIO; malware scanning service for uploads; run migrations against staging Postgres; run full integration/E2E against actual containers; restore a backup; rotate all example secrets; dependency/container vulnerability scan; define support/appeal/incident-response processes; legal/privacy review for the actual operator and jurisdiction.
